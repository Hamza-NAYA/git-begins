Les commandes qui permettent :

➔ Cloner un repository : git clone

➔ Les 3 commandes de push :git add 
			   git commit -m ""
			   git push
+ git pull 
+ git checkout -b pour créer une nouvelle branche 
+ git checkout "switcher" de branche